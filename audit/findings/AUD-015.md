# AUD-015: No accounts storage or route-level tests

- Type: Assurance gap
- State: **Fixed** — storage suite (wave 0) + route-level suite (wave 1), both enforced gates; breadth grows with each remediation wave
- Severity / rationale: High gap — every storage-behavior claim was untested
- Confidence and evidence basis: inventory confirmed at baseline; strengthened by AUD-060
- Owner: remediation waves 0–1
- Affected repositories, revisions, and local patches: `betterbase-accounts` fixes `8f44b39` (storage suite) and `6cd6f62` (route suite)
- Violated invariant / linked decision: plan Stage 4 enforced-gate criterion

## Impact and preconditions

Zero storage or route-level tests existed; the DB-skip CI comment described a skip mechanism with nothing to skip.

## Evidence and reproduction

[2026-09-20-baseline-accounts.md](../evidence/2026-09-20-baseline-accounts.md) (26 unit tests, identical with/without `DATABASE_URL`).

## Remediation (implemented)

Wave 0: per-test-schema storage harness + 27 storage tests (accounts, registration, verification, recovery, reservation invariants, cleanup) behind the enforced `just test-db`/CI gate. Wave 1: real-router route harness (`crates/api` `test_support` — real services over a throwaway schema, tower oneshot) + 15 route tests covering recovery binding, registration reservation, OAuth consent-context/recipient-binding, and the RFC 7638 known-answer vector.

## Verification

`just test-db` green (28 storage + 15 route + 26 unit); CI gate verified in wave 0. Verifier: remediation agent, 2026-09-20.
