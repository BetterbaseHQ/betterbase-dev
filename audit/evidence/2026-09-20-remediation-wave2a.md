# Evidence: Remediation wave 2a — sync rotation/authorization cluster (AUD-026, 028, 029, 030, 032, 034)

- Date / reviewer: 2026-09-20 / remediation agent (owner's code)
- Finding and invariant IDs: AUD-032 (C-09 DEK listing mismatch), AUD-030 (C-07 conflict strings), AUD-026 (C-03 rewrap CAS), AUD-029 (C-06 min key generation), AUD-028 (C-05 admin RPC boundary), AUD-034 (C-11 invitation epoch labeling); INV-03/04/05. AUD-024 (C-01) is deliberately deferred to wave 2b pending decision D-005 (revocation guarantee / fresh-key rotation).
- Repository revisions / local patch: fixes committed — `betterbase-sync` `c32f747` and `betterbase` `1a69f84`; verification ran against these trees pre-commit.
- Toolchain: rustc/cargo 1.98.1, pnpm, vitest 5, PostgreSQL 17 test container, Playwright/Chromium e2e; macOS (darwin, aarch64).
- Working directory and exact commands: `betterbase-sync` — `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, `just test-db`, `just check`; `betterbase` — `just check` (Rust + TS typecheck + vitest + 18 browser test files); platform — `just e2e` (full cycle), plus isolated `just e2e-test tests/conflict-reconcile.spec.ts`.
- Expected behavior:
  - AUD-032: `deks.get`/`deks.getFiles` results parse as ordinary `{deks:[...]}` results; rotation performs real rewraps.
  - AUD-030: `advanceEpoch` recognizes the server's `error:"conflict"` (and legacy `epoch_conflict`) and throws `EpochMismatchError` so recovery paths run.
  - AUD-026: rewrap entries carry `observed_dek`; server applies `UPDATE ... AND (observed IS NULL OR wrapped_dek = observed)` under the existing FOR UPDATE discipline; stale observation → `DekConflict` → `conflict` RPC error; client refetch+retries (bounded 4 passes).
  - AUD-029: pushes (client + federation; record DEKs and file DEKs) whose embedded wrapper epoch is below `min_key_generation` are rejected for shared spaces; tombstones without DEKs pass; personal spaces unaffected.
  - AUD-028: `membership.revoke`, `epoch.begin`, `epoch.complete` require Admin capability (SDK already issues `/space/admin` to admins; `membership.append` intentionally remains Write pending AUD-033's action-specific design).
  - AUD-034: invitation metadata carries the key's `generation`; recipient persists it as the space epoch (legacy unlabeled payloads default to 1); invite/rotate fall back to the persisted record epoch when the session map is cold.
- Observed behavior: sync — 15 suites green incl. 66 storage tests (new: rewrap CAS stale-rejected/current-observed-succeeds with concurrent wrapper intact; min-generation record pushes incl. tombstone carve-out; file-DEK min-generation) and 191 api ws tests (new: write-UCAN forbidden for revoke/epoch.begin/epoch.complete, admin-UCAN success for epoch.begin/revoke). SDK — full `just check` green incl. 199 browser tests (real WASM rotation now exercising actual rewrap paths) and new unit tests: conflict-string matrix, ordinary-result DEK parsing, invitation-generation receipt, CAS retry loop (observed-dek propagation, conflict retry with fresh observations, exhaustion, non-conflict propagation). Platform e2e: full cycle **121/121**; one transient conflict-reconcile failure on first run did not reproduce in isolation or in the subsequent full run (test has a documented concurrent-sync-race history; dev-repo HEAD commit hardens it) — recorded as a flake, watch item.
- Code review: independent reviewer — no blockers; transaction-soundness of the CAS (FOR UPDATE serialization), race-freedom of the min-generation check, exactness of the wire parsing, and minimal blast radius of the admin gate all confirmed. Should-fixes implemented in-wave: file-DEK min-generation gap (record_file now enforces), CAS retry-loop test coverage, epoch fallback to the persisted record epoch. Should-decide recorded rather than churned: legacy write-UCAN "admins" (pre-admin-UCAN chains) now get forbidden on revoke/rotate — fail-fast yields zero side effects for uniform chains, no deployments exist (pre-production); re-delegation is the remedy if ever needed. Nits deferred with rationale: epoch.complete conflict → EpochMismatchError mapping at call sites, duplicated spaces-row fetch when opts present, ~50k-record single-result ceiling on deks.get, dual-purpose observed_dek field.
- Result: pass (local + platform e2e 121/121).
- Limits: AUD-029's carve-outs are intentional (tombstones); rewrap CAS protects the wrapper-vs-ciphertext pairing, not historical plaintext; AUD-024 (revoked-subscriber broadcasts + forward-derived rotation keys) remains open pending D-005; reviewer nits above deferred.
- Seed / reproduction: `just test-db` (sync), `just check` (SDK), `just e2e` (platform).
