# Evidence: Betterbase SDK baseline check suite (native + TS + browser)

- Date / reviewer: 2026-09-20 / SDK baseline-execution agent (automated defensive security audit)
- Finding and invariant IDs: none — baseline execution record per `audit/baseline.md` "Execution scope" row "SDK native, TypeScript and browser checks" (previously reported executed with no recorded evidence; re-run and recorded here)
- Repository revisions / local patch: `betterbase` @ `e3a9ad167d8d3cf0d8310304716e5c4e75861a47`, worktree clean before and after the run (verified via `git rev-parse HEAD` + `git status --short`)
- Lockfile hashes / generated artifact provenance: `pnpm install` reported "Already up to date" — `js/pnpm-lock.yaml` not regenerated, not hashed (unchanged per git status). Prebuilt WASM packages under `js/` were used as-is; no `build:wasm` executed by any recipe run. Cargo `target/` was fully warm (0 `Compiling` lines in the whole run).
- Tool versions / operating system / browser: rustc 1.98.1 (48a229cea 2026-09-01), cargo 1.98.1 (797e8a9bc 2026-08-05), node v24.21.0, pnpm 12.4.2 — all via mise shims (`~/.local/share/mise/shims/{rustc,cargo,node,pnpm}`, canonical per repo toolchain). vitest 5.0.1. OS: macOS (darwin). Browser: Chromium only, headless (see below).
- Non-secret configuration / isolated services: none used. Pure offline test suites; no docker/dev services touched, no secrets in input or output. Vitest browser run started a local API listener on an ephemeral loopback port (`http://localhost:63315/`) — tooling-internal, not a service under test.
- Working directory and exact command or source-inspection method: `/Users/nchapman/Code/betterbase-dev/betterbase`. Commands, in order:
  1. `just check` — expands to `cargo fmt --all`; 7x `cargo clippy` (5 native crates `--all-targets`, 2 wasm crates `--target wasm32-unknown-unknown`, all with `-- -D warnings`); `cargo test --workspace --exclude betterbase-wasm --exclude betterbase-db-wasm`; `cd js && pnpm install && pnpm check` where `pnpm check` = `prettier --write . && pnpm typecheck && pnpm test && pnpm test:browser` (typecheck = `tsc --noEmit`; test = `vitest run --passWithNoTests`; test:browser = `vitest run --passWithNoTests --config vitest.browser.config.ts`)
  2. `just test-browser` — `cd js && pnpm vitest run --config vitest.browser.config.ts`
  Browser selection confirmed from `js/vitest.browser.config.ts`: `browser: { enabled: true, provider: playwright(), instances: [{ browser: "chromium" }], headless: true }`, include `browser-tests/**/*.test.ts`, 15s per-test timeout. Chromium is the only instance — no other browser runs.
- Expected behavior: all checks pass with zero test failures; tree remains clean (recipes include format/lockfile-mutating steps that are no-ops on an already-conformant tree).
- Observed behavior: everything passed. Full per-suite counts below.
- Exit code / assertions / skipped tests:
  - **Rust native (`just test`)**: exit 0. 21 test binaries (16 with tests + 5 empty doc-test entries), **1379 passed / 0 failed / 0 ignored** total. Per-binary (binary — passed): betterbase_auth lib 46; betterbase_crypto lib 138; betterbase_db lib 149; db integration: collection 78, crdt_smoke 6, index 58, middleware 47, patch 79, query 179, reactive 61, scenarios 40, schema 167, storage 181, sync 71; betterbase_discovery lib 27; betterbase_sync_core lib 52; doc-tests (auth, crypto, db, discovery, sync_core) 0 each.
  - **Design exclusion (not a skip)**: native `cargo test` excludes `betterbase-wasm` and `betterbase-db-wasm` (justfile `--exclude` flags); those crates are covered by the browser suite (real WASM in real browser APIs). `sqlite-wasm-vfs` is vendored wasm32-only and likewise not tested natively.
  - **Ignored/skipped test names**: none in any suite — cargo reported `0 ignored` in every binary; vitest summaries reported no skipped tests (grep for "skip" in logs matched only *passing test names containing the word "skip"*, e.g. `key_extraction::tests::skips_ec_entries`, which are passing assertions, not skips).
  - **Clippy (`just lint`)**: exit 0 across all 7 invocations with `-D warnings`; **0 warnings** (no warning lines in output; no `generated N warnings` lines).
  - **fmt / prettier**: exit 0; `cargo fmt --all` and `prettier --write .` produced zero modifications (all prettier targets reported "(unchanged)").
  - **TS typecheck**: `tsc --noEmit` exit 0, no diagnostics.
  - **vitest node (`pnpm test` inside check-js)**: exit 0 — **27 test files passed (27), 467 tests passed (467)**, 0 failed, 0 skipped; duration 2.59s.
  - **vitest browser inside check-js (`pnpm test:browser`)**: exit 0 — **18 test files passed (18), 199 tests passed (199)**, 0 failed, 0 skipped; duration 14.44s; Chromium headless.
  - **`just test-browser` (step 2, explicit re-run)**: exit 0 — **18 test files passed (18), 199 tests passed (199)**, 0 failed, 0 skipped; duration 14.58s (total recipe wall time 15.3s incl. vite/vitest startup). Results identical to the browser phase of check-js, as expected (same config).
- Result: **pass** (all suites).
- Seed / schedule / reproduction steps: deterministic — `cd betterbase && git status --short` (must be empty) && `just check` && `just test-browser`. No seeds, no network dependencies, no ordering sensitivity observed.
- Sanitized output or artifact links: raw logs at `/var/folders/v0/t85z9j5x4dn0b__6vpm6__4c0000gn/T/opencode/audit-logs/just-check.log` and `.../just-test-browser.log` (temporary, outside the repo). Nothing token-like appeared in any output; nothing redacted.
- Churn/revert record: **none required.** Post-run `git rev-parse HEAD` = `e3a9ad167d8d3cf0d8310304716e5c4e75861a47` (unchanged) and `git status --short` empty. `cargo fmt` made no edits, `prettier --write .` reported every file "(unchanged)", `pnpm install` reported "Already up to date" (`js/pnpm-lock.yaml` untouched). No `git restore` was needed; tree was never dirty.
- Durations: `just check` total ≈ 23s wall (18:49:38–18:50:01 local; cargo cache fully warm — 0 compile lines, every clippy/test "Finished" in ≤0.14s); browser phase inside it 14.44s. `just test-browser` 15.3s wall. First-cold builds would be far longer; these numbers reflect a warm `target/` and installed node_modules.
- Environment notes: no environment-blocked steps; Playwright/Chromium binaries were present and functional (both browser runs completed). One benign non-failure note: vitest prints `Plugin "vitest:mocks:interceptor" defines Vite-specific hooks (configureServer)... These hooks will be ignored.` before browser runs — tooling warning, not a product failure.
- Limits:
  - Warm-cache execution: no cold-build verification (compile warnings or platform-specific build issues would surface identically, but build time is unmeasured).
  - Native suite by design does not execute wasm-crate code (`betterbase-wasm`, `betterbase-db-wasm`, `sqlite-wasm-vfs`); browser coverage uses prebuilt WASM artifacts shipped in `js/` rather than a fresh `build:wasm` from this revision's Rust sources — WASM-binary/Rust-source drift at this HEAD is not tested by this run.
  - Doc-tests: 0 exist across the 5 pure crates (recorded as 0/0, not a gap in execution).
  - `just bench` / `bench-browser`, fuzzing, and interop-vs-live-server scenarios are outside this baseline's scope per baseline.md.
