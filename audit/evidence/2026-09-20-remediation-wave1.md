# Evidence: Remediation wave 1 — account-takeover criticals (AUD-003, AUD-005, AUD-006)

- Date / reviewer: 2026-09-20 / remediation agent (owner's code)
- Finding and invariant IDs: AUD-003 (A-01, recovery binding), AUD-005 (A-03, consent key delivery), AUD-006 (A-04, registration overwrite); INV-04/05. AUD-015 route-level portion addressed alongside.
- Repository revisions / local patch: fixes committed — `betterbase-accounts` `6cd6f62` (on `8f44b39`); verification ran against this tree pre-commit. Web SPA changes ship in the same repo/binary.
- Lockfile hashes / generated artifact provenance: `Cargo.lock` updated (sqlx dev-dependency for the api route-test harness); `.sqlx` regenerated via `cargo sqlx prepare --workspace` against a migrated test container (76 files, +3: CAS existence probe, reservation-conflict path unchanged queries, cleanup delete).
- Tool versions / operating system: rustc/cargo 1.98.1, sqlx-cli 0.9.0 (installed this wave), just 1.58.0, pnpm 12.4.2, node v24.21.0, PostgreSQL 17 container, macOS (darwin, aarch64).
- Non-secret configuration / isolated services: repo's own `betterbase-accounts-test-db` container; route tests use real services (in-memory JWT HMAC/ES256 keys per test, generated OPAQUE ServerSetup, CAP disabled, DevMailer) over a throwaway schema.
- Working directory and exact command or source-inspection method: `/Users/nchapman/Code/betterbase-dev/betterbase-accounts` — `just check` (fmt, clippy `-D warnings`, tests, web `pnpm check`), `just test-db` (enforced live gate), plus targeted reruns during development.
- Expected behavior:
  - AUD-003: `recover/init` resolves the account from the email in the verified token; a request email that does not match is rejected before the JTI is consumed; the verified email drives the rate limit and lookup.
  - AUD-006: a verified email cannot reserve an existing username (409, handler + storage invariant), and signup finalize is compare-and-set on `opaque_record IS NULL`; recovery paths remain intentionally overwrite-capable.
  - AUD-005: authorize redirects carry only the signed state; the consent page's display and wrapping context comes from the new auth-gated `GET /oauth/consent-context`; consent binds `keys_jwe`+`keys_jwk_thumbprint` to the RFC 7638 thumbprint of the signed `keys_jwk` and requires delivery for the sync flow.
- Observed behavior: 15 route-level tests (`crates/api` via new `test_support` real-router harness), 5 new/updated storage tests, web suite 175 passed / 2 hardcoded skips, `just check` and `just test-db` both green (28 storage + 15 route + 26 unit). Tests assert the security properties: cross-email token rejected without JTI consumption and with victim credentials untouched; username-claim rejected 409 with credentials untouched and token reusable; thumbprint mismatch/partial-pair/no-recipient/sync-silent-downgrade all rejected; consent-context returns signed fields and requires auth.
- Exit code / assertions / skipped tests: all suites exit 0; no unexpected skips (web: 2 pre-existing hardcoded `it.skip`).
- Result: pass. Platform e2e executed after the fix commit: `just e2e` full cycle 121/121 passed (2026-09-20), covering the co-deployed SPA/server OAuth consent, rotation, revocation, and federation journeys.
- Code review: independent reviewer found no blockers; three should-fix findings were fixed in-wave (verification-token burn on username conflict → check moved before JTI consumption with a retry-succeeds test; abandoned reservations squatting forever → `cleanup_unregistered_accounts` storage method + app cleanup-loop task with a backdated-reservation test; missing `validate_email` panic path in `recover/init` → validated like `password/init`). Also implemented reviewer suggestions: silent key-delivery downgrade for the sync flow now rejected; RFC 7638 known-answer vector pinned; `password/init` token/email comparison switched to canonical form.
- Seed / schedule / reproduction steps: `just db-down; just test-db`; each route test builds its own schema/router.
- Sanitized output or artifact links: outputs quoted inline; no secrets.
- Limits: browser-level tamper test from the original remediation note (mutate each duplicated URL parameter) is covered server-side (URL duplicates no longer exist) and by the Rust binding tests, but no component-level SPA test was added (no component-testing infra in the web suite — an infrastructure decision, not a one-off test); SDK-side `key_extraction.rs` client-ID non-enforcement noted in review 01 remains a follow-up (defense in depth at the SDK boundary, not required for this fix); `finalize_registration` (non-CAS) now has no production callers — flagged for trait cleanup in a later wave; cached-SPA-bundle rollout caveat recorded (binary ships both sides; verify `index.html` caching on deploy).
