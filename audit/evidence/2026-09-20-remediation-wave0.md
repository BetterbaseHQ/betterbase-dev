# Evidence: Remediation wave 0 — accounts test gates (AUD-060, AUD-001, AUD-002, AUD-015 partial)

- Date / reviewer: 2026-09-20 / remediation agent (owner's code)
- Finding and invariant IDs: AUD-060 (test-db gate cannot execute), AUD-001 (CI omits `DATABASE_URL`), AUD-002 (`check-all` omits inference, json-joy-rs, platform E2E), AUD-015 (no storage tests — storage portion only).
- Repository revisions / local patch: fixes committed — `betterbase-accounts` `8f44b39` (on `b7cda8872ee0`) and `betterbase-dev` `982b3ff` (on `7a36c07c7fc8`); verification ran against these trees pre-commit. No other repos touched.
- Lockfile hashes / generated artifact provenance: no lockfiles or generated artifacts changed; `.sqlx/` offline metadata unmodified; web `pnpm-lock.yaml` unmodified (`pnpm install` no-op).
- Tool versions / operating system: rustc/cargo 1.98.1, just 1.58.0, pnpm 12.4.2, node v24.21.0, PostgreSQL 17 (`postgres:17-alpine` container), macOS (darwin, aarch64).
- Non-secret configuration / isolated services: repo's own `betterbase-accounts-test-db` container (loopback 15433, disposable credentials from the committed justfile). Dev/e2e stacks untouched.
- Working directory and exact command or source-inspection method: `/Users/nchapman/Code/betterbase-dev/betterbase-accounts` — `just check` (fmt, clippy `-D warnings`, `cargo test --workspace`, web `pnpm check`); `just db-down` then `just test-db` (fresh container); `BB_TEST_REQUIRE_DB=1 cargo test -p betterbase-accounts-storage --lib` with no `DATABASE_URL` (enforcement check). `/Users/nchapman/Code/betterbase-dev` — `just -n check-all` (dry-run), `just --list`.
- Expected behavior: `just test-db` migrates and runs the suite against live PostgreSQL with skips impossible; `just check`/`just test` remain green without a DB; CI workflow provisions PostgreSQL and sets `DATABASE_URL` + `BB_TEST_REQUIRE_DB=1`; root `check-all` covers every component repo and `check-platform` composes it with `just e2e`.
- Observed behavior:
  - Changes: new `crates/storage/src/postgres/test_support.rs` (per-test schema `test_{uuid}`, `search_path`-scoped pool, `sqlx::migrate!()` at harness setup, panics when `BB_TEST_REQUIRE_DB=1` and `DATABASE_URL` missing/unreachable; graceful skip otherwise); 23 storage tests across `accounts.rs`, `registration.rs`, `verification.rs`, `recovery.rs`; `test-db` recipe now sets `SQLX_OFFLINE=true BB_TEST_REQUIRE_DB=1 DATABASE_URL=...` (compile-time macros use committed `.sqlx` metadata; queries execute live); CI adds a `postgres:17-alpine` service and env on the test step; root `check-all` adds `betterbase-inference` + `json-joy-rs`, new `check-platform: check-all` + `e2e`; AGENTS.md comments updated both repos.
  - During development the live gate caught two real contract details: the schema CHECK restricting verification `purpose` to ('registration','recovery'), and `create_verification_code` hardcoding `attempts = 0` on insert (tests updated to exercise the real lockout path via `increment_verification_attempts`).
- Exit code / assertions / skipped tests: `just check` exit 0 (fmt clean, clippy clean, 49 Rust tests pass — 26 pre-existing + 23 new, web 175 passed / 2 hardcoded skips, no churn). `just test-db` exit 0 on a fresh container: all 49 pass, storage suite runs live (previously exit 1 with ~80 sqlx compile errors and zero storage tests). Enforcement check: every storage test panics at `test_support.rs` when `BB_TEST_REQUIRE_DB=1` and no `DATABASE_URL` (suite fails as designed). Root `just -n check-all` and `just --list` parse cleanly.
- Result: pass (local). CI run pending push (workflow change not yet exercised on GitHub Actions); `check-all`/`check-platform` full execution not run this wave (component suites for inference/json-joy-rs unchanged — baseline evidence already records them green).
- Seed / schedule / reproduction steps: `just db-down; just test-db` on any machine with Docker; enforcement: `BB_TEST_REQUIRE_DB=1 cargo test -p betterbase-accounts-storage --lib` with `DATABASE_URL` unset.
- Sanitized output or artifact links: outputs quoted inline above; no secrets involved (test credentials are committed in the justfile).
- Limits: GitHub Actions run of the modified workflow pending push; the two mid-development test-contract corrections are recorded above rather than hidden; AUD-015's route-level/API test gap remains open (planned with wave 1 regression tests).
