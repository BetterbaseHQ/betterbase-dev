# Evidence: Betterbase accounts baseline checks, including the real-PostgreSQL gate (AUD-015 / A-13, AUD-001)

- Date / reviewer: 2026-09-20 / accounts baseline-execution agent (defensive security audit, owner's code)
- Finding and invariant IDs: AUD-015 (A-13, assurance gap: no accounts storage or route-level tests; DB-skip comment overstates what enabling PostgreSQL adds), AUD-001 (CI omits `DATABASE_URL`, DB-dependent tests may skip). Baseline execution-scope row: "Accounts checks, including real PostgreSQL — partially evidenced".
- Repository revisions / local patch: `betterbase-accounts` at `b7cda8872ee09cfad44f4186ff40096ee3c95483` (verified `git rev-parse HEAD` before and after; worktree clean before and after — no local patch).
- Lockfile hashes / generated artifact provenance: no lockfile or generated artifacts changed. Web checks ran against the committed `web/pnpm-lock.yaml` (`pnpm install` produced no modification; `prettier --write .` reported every file unchanged). Rust workspace built from the committed `Cargo.lock` and the committed `.sqlx/` offline query-metadata directory (present at repo root).
- Tool versions / operating system / browser: rustc 1.98.1 (48a229cea 2026-09-01), cargo 1.98.1 (797e8a9bc 2026-08-05), node v24.21.0, pnpm 12.4.2, just 1.58.0, macOS (darwin, aarch64). Test DB: `postgres:17-alpine` image `sha256:f02121de6f74d30d8a94cd1d9584125e2178d7e6c377d8130112d4e52d867995`, server/psql 17.11 (both via `docker exec betterbase-accounts-test-db psql --version` and standalone `docker run --rm postgres:17-alpine psql --version`). Web test runner: vitest 5.0.1. No browser used.
- Non-secret configuration / isolated services: repo's own test-db container per justfile — name `betterbase-accounts-test-db`, loopback port 15433, user/db `accounts`/`accounts_test`, disposable credentials from the committed justfile (`postgres://accounts:accounts@localhost:15433/accounts_test?sslmode=disable`). Dev/e2e stacks and the audit container `betterbase-audit-postgres` (127.0.0.1:25432) were not touched; verified still running afterward. E2E recipes not run (out of scope). No `DATABASE_URL` in the ambient environment for the no-DB runs.
- Working directory and exact command or source-inspection method: `/Users/nchapman/Code/betterbase-dev/betterbase-accounts`. Commands, in order:
  1. `git rev-parse HEAD && git status --short` (pre) → `b7cda8872ee09cfad44f4186ff40096ee3c95483`, clean.
  2. `cargo test --workspace -- --list` (no `DATABASE_URL`) → 26 tests listed.
  3. `just check` (= `cargo fmt --all`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `cd web && pnpm install && pnpm check`).
  4. `git status --short` (post-check churn check).
  5. `just test-db -- --list` (spins up test-db, runs the listing with `DATABASE_URL`, aborts on compile failure before `db-down`; container left up by the failed recipe).
  6. `just test-db` (exact designed recipe: full `cargo test --workspace` with `DATABASE_URL`).
  7. `DATABASE_URL="postgres://accounts:accounts@localhost:15433/accounts_test?sslmode=disable" SQLX_OFFLINE=true cargo test --workspace` (differential: compilation forced to the offline `.sqlx` metadata so the suite can complete with a live DB configured).
  8. `just db-down` → `docker rm -f betterbase-accounts-test-db`; container removed. Audit container untouched.
  9. `git rev-parse HEAD && git status --short` (post) → unchanged, clean.
  Source inspection: `rg -n 'DATABASE_URL' crates/ bins/ --type rust`; `rg -c 'cfg\(test\)' crates/storage/src/`; `rg -n 'mod tests' crates/storage/src/`; `rg -n 'skipIf|describe\.skip|it\.skip|test\.skip' web/test/`; `docker exec betterbase-accounts-test-db psql -U accounts -d accounts_test -tAc "SELECT count(*) FROM information_schema.tables WHERE table_schema='public'"`.
- Expected behavior: `just check` passes all four stages without modifying the worktree. `just test-db` is documented ("Run tests against a real PostgreSQL database") and AGENTS.md/justfile comments claim "DB tests auto-skip without DATABASE_URL", implying DB-gated tests exist and run when `DATABASE_URL` is set. The audit (AUD-015/A-13) predicts ~26 unit tests and zero storage tests regardless of `DATABASE_URL`.
- Observed behavior:
  - **`just check` — exit 0.** fmt: no file modified. clippy: clean under `-D warnings`. Rust tests (no `DATABASE_URL`): 26 passed / 0 failed / 0 ignored across 10 unittest targets; nonzero targets: `betterbase_accounts_auth` 12 (`es256::tests` ×4, `jwt::tests` ×5, `opaque::tests` ×3) and `betterbase_accounts_core` 14 (`email::tests` ×4, `identity::tests` ×7, `username::tests` ×3); zero tests in storage, api, app, email, cap, and all three bins. All doctest targets: 0 tests. check-web: `tsc -b` clean; vitest 8 files passed + 1 skipped (9), 175 tests passed / 2 skipped (177) — the 2 skips are hardcoded `it.skip` Go-server OPAQUE integration cases at `web/test/lib/opaque.integration.test.ts:46` and `:169` (skip-by-design, not environment-gated). Vite `configLoader: 'native'` deprecation warning about `__dirname` in `vite.config.ts:18` (non-fatal).
  - **`just test-db` — exit 1 (fresh container).** `db-start` creates a fresh `postgres:17-alpine` whose `accounts_test` DB has **0 tables** (verified via information_schema; the recipe applies no migrations). With `DATABASE_URL` set, sqlx compile-time query checking (`sqlx::query!` / `query_as!`) validates against that live DB instead of the `.sqlx` offline metadata and fails: "error: aborting due to 80 previous errors", e.g. `relation "accounts" does not exist` (`crates/storage/src/postgres/accounts.rs:43`), `relation "used_verification_tokens" does not exist` (`crates/storage/src/postgres/verification.rs:173`). The 26 unit tests that do run all pass (12 + 14, identical set), then the `betterbase_accounts_storage` doctest target aborts compilation; `set -e` stops the recipe before `db-down`, leaving the container up.
  - **DATABASE_URL differential (test discovery):** `cargo test --workspace -- --list` without `DATABASE_URL` vs with (via `just test-db -- --list`): test-name lists identical — 26 tests, zero difference in test names. Only diff lines: the justfile's recipe preamble, and one missing `0 tests, 0 benchmarks` line in the with-DB list because the storage doctest target aborted before printing it. **No DB-gated test exists anywhere**: source inspection shows `DATABASE_URL` is read only by non-test code (`bins/oauth-client/src/main.rs:64`, `crates/app/src/lib.rs:58`), and `crates/storage/src/` contains zero `#[cfg(test)]` modules.
  - **With-DB run forced to compile offline (`SQLX_OFFLINE=true` + live `DATABASE_URL`): exit 0** — 17 targets all ok; again only auth (12) and core (14) have tests; 26 passed / 0 failed / 0 ignored; zero storage or route-level tests executed against PostgreSQL.
- Exit code / assertions / skipped tests: `just check` 0; `cargo test --workspace -- --list` (no DB) 0 (26 tests); `just test-db -- --list` 1; `just test-db` 1; forced-offline with-DB run 0 (26 passed). No assertion failures; no `ignored` tests in any Rust run (`0 ignored` in every summary line); web: 2 hardcoded `it.skip`.
- Result: **pass for `just check` (fmt/lint/test/check-web all green, no churn); fail for `just test-db` as designed (recipe cannot complete on a fresh container — compile-time sqlx failure, 80 errors, storage doctest target); AUD-015/A-13 confirmed and strengthened.**
- Seed / schedule / reproduction steps: fresh docker environment with the `postgres:17-alpine` image present; run `just db-down` (clean slate), then `just test-db` — reproduces the 80 sqlx compile errors on any fresh container because `db-start` never applies migrations. For the offline-forced variant, keep the container up (`just db-start` is `[private]`; rerun `just test-db` and it aborts after `db-start`) and run `DATABASE_URL="postgres://accounts:accounts@localhost:15433/accounts_test?sslmode=disable" SQLX_OFFLINE=true cargo test --workspace`.
- Sanitized output or artifact links: raw logs kept at `/tmp/bb-accounts-just-check.log`, `/tmp/bb-accounts-test-db-run.log`, `/tmp/bb-accounts-test-list-nodb.txt`, `/tmp/bb-accounts-test-list-db.txt`, `/tmp/bb-accounts-test-db-offline.log` (session-temp; key lines quoted above and in the final report).
- Limits: (1) Unit-test binaries were warm-cached from the no-DB build when the DB runs executed; cargo does not rebuild on proc-macro-read env changes. On a cold cache with `DATABASE_URL` set, the storage rlib compilation itself would hit the same 80 macro errors (same expansion, observed in the rustdoc/doctest compilation); the conclusion is unchanged, only the failing target moves earlier. (2) `just test-db`'s only viable paths are `SQLX_OFFLINE=true` or a container previously migrated by hand (state persists across `docker start`); neither is part of the recipe. (3) The with-DB listing came from the test binaries that ran before the storage doctest abort; identical name set to the no-DB listing, so the differential is complete. (4) No route-level/integration tests exist to run in any configuration; nothing about HTTP behavior, OPAQUE flows, or OAuth against PostgreSQL is exercised by these suites — that is the AUD-015 gap itself, not a limit of this run. (5) Churn/revert record: none needed — `git status --short` was empty after `just check` (fmt made no edits; `pnpm install` did not touch the lockfile); no `git restore` required; final status clean at the pinned HEAD.

## Per-suite summary

| Suite | Command | Result | Counts |
| --- | --- | --- | --- |
| fmt | `cargo fmt --all` | pass | 0 files modified |
| lint | `cargo clippy --workspace --all-targets -- -D warnings` | pass | 0 warnings (denied) |
| test (no DB) | `cargo test --workspace` | pass | 26 passed / 0 failed / 0 ignored (auth 12, core 14, all other targets 0) |
| check-web | `pnpm install && pnpm check` | pass | tsc clean; vitest 175 passed / 2 skipped (177), 1 of 9 files skipped (hardcoded `it.skip`) |
| test discovery, no DB | `cargo test --workspace -- --list` | pass | 26 tests |
| test discovery, with DB | `just test-db -- --list` | fail (compile) | same 26 names; storage doctest target aborted (80 sqlx errors) |
| real-PostgreSQL gate | `just test-db` | fail (recipe) | 26 unit tests pass (same set), then compile abort; `db-down` never reached |
| with DB, offline macros | `DATABASE_URL=... SQLX_OFFLINE=true cargo test --workspace` | pass | 26 passed / 0 failed / 0 ignored; **zero storage tests executed against PostgreSQL** |

## AUD-015 / A-13 verdict

Confirmed, with one strengthening correction. Exactly 26 unit tests exist across 6 test modules (matching the audit inventory); the with/without-`DATABASE_URL` test-name diff is empty — **no DB-gated test exists at all** ("DB tests auto-skip without DATABASE_URL" in the justfile/AGENTS.md/CI describes a skip mechanism that has nothing to skip). Stronger than predicted: on a fresh container the real-PostgreSQL gate **cannot even complete compilation**, because `test-db` never applies migrations and sqlx compile-time query validation against the empty live DB yields 80 errors in the storage crate. Even when compilation is forced offline with a live DB configured, zero storage or route-level tests run. AUD-001's CI observation (DB-dependent tests may silently skip) is moot in practice — there is nothing to skip.
