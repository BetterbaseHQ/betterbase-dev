# Evidence: <scope and case>

- Date / reviewer:
- Finding and invariant IDs:
- Repository revisions / local patch:
- Lockfile hashes / generated artifact provenance:
- Tool versions / operating system / browser:
- Non-secret configuration / isolated services:
- Working directory and exact command or source-inspection method:
- Expected behavior:
- Observed behavior:
- Exit code / assertions / skipped tests:
- Result: pass | fail | blocked | source-only | not run
- Seed / schedule / reproduction steps:
- Sanitized output or artifact links:
- Limits: mocks, unsupported environments, untested paths, or unreproduced claims:

Never include real credentials, tokens, encryption keys, or private user data. Preserve enough synthetic setup to reproduce the result.
